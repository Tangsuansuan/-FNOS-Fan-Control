{
    "type": "http",
    "path": "/api/status",
    "expect_status": [200],
    "startup_timeout_seconds": 90,
    "post_install_warmup_seconds": 10,
    "skip_arch": [],
    "note": "Checks if the FastAPI backend is responding. Longer timeout for first-start venv fallback."
}
